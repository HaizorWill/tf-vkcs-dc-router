# Simple advanced Sprut router with BGP peering

This example configures a single simple Direct Connect router connected to provider's network.

The router has an SNAT port, which is automatically assigned to a port connected to provider's network, a DNAT rule, BGP instance and an abstract BGP neighbor.

